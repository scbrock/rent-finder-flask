name: Deploy to Render

on:
  push:
    branches:
      - clean_build

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger Render deploy
        run: curl -s --fail "${{ secrets.RENDER_DEPLOY_HOOK }}" -o /dev/null
